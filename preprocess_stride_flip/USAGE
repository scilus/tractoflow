Scilpy dMRI preprocess pipeline
================================

Preprocess data
Modify the strides to be in 1,2,3,4 and flip gradients if needed.

USAGE

nextflow run main.nf [OPTIONAL_ARGUMENTS] (--root or --subject)

DESCRIPTION

--root=/path/to/[root]              Root folder containing multiple subjects

                                    [root]
                                    ├── S1
                                    │   ├── A1
                                    │   │   ├── dwi.nii.gz
                                    │   │   ├── bval
                                    │   │   ├── bvec
                                    │   │   ├── rev_b0.nii.gz  (optionnal)
                                    │   │   └── t1.nii.gz
                                    │   └── A2
                                    │   │   ├── dwi.nii.gz
                                    │   │   ├── bval
                                    │   │   ├── bvec
                                    │   │   ├── rev_b0.nii.gz  (optionnal)
                                    │   │   └── t1.nii.gz
                                    └── S2
                                        ├── A1
                                        │   └── *.nii.gz
                                        └── A2
                                            └── *.nii.gz

--subject=/path/to/[subject]        Subject folder containing:

                                    [subject]
                                    ├── dwi.nii.gz
                                    ├── bval
                                    ├── bvec
                                    ├── rev_b0.nii.gz  (optionnal)
                                    └── t1.nii.gz

Files should be compressed Nifti files (.nii.gz)

OPTIONAL ARGUMENTS (current value)

--flipx                                     Flip gradients in x ($flipx).

--flipy                                     Flip gradients in y ($flipy).

--flipz                                     Flip gradients in z ($flipz).

--output_dir                                Directory where to write the final results.
                                            By default, will be in "./preprocess"

NOTES

The `scilpy/scripts` folder should be in your PATH environment variable

The intermediate working directory is, by default, set to "./work".
To change it, use the '-w WORK_DIR' argument.

The default config file is preprocess_stride_flip/nextflow.config.
Use `-C config_file.config` to specify a non-default configuration file.
The `-C config_file.config` must be insert after the nextflow call 
like `nextflow -C config_file.config run ...`.
